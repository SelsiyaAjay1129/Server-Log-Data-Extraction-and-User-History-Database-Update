EMAIL AND DATE EXTRACTION TOOL
This repository contains a Python script that extracts email addresses and dates from a log file, transforms the data, and saves it into both MongoDB and MySQL databases.

FEATURES
Extracts email addresses and dates from log files.
Transforms extracted data into a suitable format.
Saves the data into MongoDB and MySQL databases.
Handles large data inserts by using batch processing.